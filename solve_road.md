#### Step 1: 初始化阶段

* **输入：**
  * `equations`：模块的本构方程列表（sympy 表达式）
  * `knowns`：已知变量和参数的字典 `{symbol: value}`
* **输出：**
  * `solved_vars`：已求解的变量及其值的字典


#### Step 2: 解析符号信息

* 使用 `sympy.symbols()` 或 `.free_symbols` 提取每个方程中的所有符号
* 建立映射关系：

  * `eq_to_vars`: 每个方程 → 其中涉及的变量集合（包括未知和已知）
  * `all_vars`: 所有变量集合（用于后续筛选未知变量）

#### Step 3: 筛选未知变量

* 通过 `unknown_vars = all_vars - knowns.keys()` 得到所有未知变量
* 更新 `eq_to_unknowns,unknowns_to_eq(双向索引)`: 每个方程 → 该方程中仍然未知的变量集合
* 

#### Step 4: 迭代求解流程（贪心 + 回代）

* **循环条件： **当还有未解方程存在时，并设置最大迭代次数防止死循环
* **每轮操作：**
  1. 遍历所有方程
  2. 对于每个方程，查看其 `eq_to_unknowns[eq]` 是否只有一个未知数
     * 若是：
       * 使用 `sympy.solve(eq, var)` 求解该未知数，若不可解，则记录方程。若有多解，先用第一个后续优化。
       * 将结果代入 `knowns` 并记录到 `solved_vars`
       * 更新所有方程中的该变量为已知值（即进行 substitution）
       * 从方程集中删除当前方程
       * **跳出本轮循环，重新开始遍历 ** （因为 substitution 可能导致其它方程变为可解）
     * 否则：
       * 跳过，继续下一方程
  3. 如果一轮遍历中没有任何方程被求解：
     * 转入  **联合求解模式 **

#### Step 5: 结束条件

* 若方程集为空 → 返回 `solved_vars`
* 若无法进一步单步求解 → 视情况使用联合求解或其他方式
